## ADDED Requirements

### Requirement: Client dashboard provides a unified overview
The system SHALL display a client dashboard that includes summary cards for key metrics, a client type list, recent activity, and vendor recommendations.

#### Scenario: Client opens dashboard overview
- **WHEN** a client navigates to the client dashboard screen
- **THEN** the UI loads metric cards, a client type panel, a recent activity feed, and vendor recommendation cards
