## ADDED Requirements

### Requirement: Client activity feed shows recent events
The system SHALL display a recent activity feed on the client dashboard that lists recent client actions and project updates.

#### Scenario: Recent activity feed shows updates
- **WHEN** the dashboard loads
- **THEN** the activity feed lists the latest 5 client-related updates with timestamps and summary text
