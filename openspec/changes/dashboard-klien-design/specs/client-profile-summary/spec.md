## ADDED Requirements

### Requirement: Client profile summary displays identity and status
The system SHALL show a client profile summary panel that includes client name, client type label, project status counts, reputation, and quick action links.

#### Scenario: Profile summary is visible
- **WHEN** the client dashboard is rendered
- **THEN** the profile summary displays the client name, type, project counts, reputation score, and at least one quick action button
